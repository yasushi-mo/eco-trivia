{
  "name": "eco-trivia",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ]
}
