# 1. 依存関係のディレクトリ
node_modules

# 2. ビルド成果物（tscやWebpackなどで生成されるファイル）
# shared/backend/frontend のdistディレクトリは全て除外
dist
**/*.d.ts
!shared/dist # sharedパッケージのビルド成果物は、package.jsonのfilesで管理されるため、通常は除外不要ですが、明示的に含めるか否かをチームで決定します。
             # モノレポ内リンクでは追跡不要ですが、安全策として全て除外が基本です。
**/*.tsbuildinfo

# 3. ログファイルと一時ファイル
*.log
npm-debug.log*

# 4. OS/エディタ関連
.DS_Store
.vscode/

# 5. 環境ファイル
.env
.env.*

# 6. Prisma
*generated
